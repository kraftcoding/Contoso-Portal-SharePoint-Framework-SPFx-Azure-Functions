trigger: none

schedules:
- cron: '0 6 * * 1-5'
  displayName: Daily quality build
  branches:
    include:
    - main
    
pool:
  vmImage: ubuntu-latest

stages:

  - stage: build
    jobs:
    
    - template: './jobs/job-build-artifacts-all-sonar.yaml'