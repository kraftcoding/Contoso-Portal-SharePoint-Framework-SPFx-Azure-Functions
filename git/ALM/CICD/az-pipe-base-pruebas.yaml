trigger: none

pool:
  vmImage: ubuntu-latest

stages:

  - stage: build
    jobs:
    
    - template: ./jobs/job-build-artifacts-all.yaml
      
    - template: ./jobs/job-deploy-artifacts.yaml
      parameters:
        environment: 'devcdf'
        pool:
          vmImage: ubuntu-latest
        depends_on: []
